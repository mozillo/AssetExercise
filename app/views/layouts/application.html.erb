<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Assets Manage" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Assets Manage" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">

      <div class="col-md-2">
          <ul class="nav nav-sidebar affix">
            <li><%=link_to "Settings",departments_path %></li>
            <li><%=link_to "Assets Manages", asset_manages_path%></li>
            <li><%=link_to "Logs", logs_path %></li>
            <li><a href="#">Export</a></li>
          </ul>
      </div>

      <div class="col-md-10">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
